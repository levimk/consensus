server:
  hostname: localhost
  port: 8082
  keys:
    private: "my-private-key"
    public: PUBKEY
  communication:
    protocol: tcp
  peers:
    [
      "1:localhost:8080",
      "2:localhost:8081",
      "4:localhost:8083",
      "5:localhost:8084",
    ]
